# Epic 1 회고 — 데이터 기반 & 지표 조회 (2026-07-10)

## 요약

- **완료**: 7/7 스토리 done (1-1 스캐폴딩, 1-2 재무(DART), 1-3 시세(KRX), 1-4 매크로(ECOS), 1-5 밸류업공시(DART), 1-6 지분구조(DART), 1-7 지표VIEW+API)
- **검증**: pytest 78 passed (fixture 기반, 라이브 키 없이 CI 가능)
- **산출물**: 원천 5테이블(company·financials·prices·macro_indicator·valueup_plan·ownership) + `valuation_metrics` SQL VIEW + `/metrics` API
- **참가**: 프로젝트 리드(사용자) + AI 개발/리뷰(BMAD 3-layer + GPT 교차검증)

## 잘된 것

1. **어댑터 패턴 재사용의 복리효과** — 1-2의 `SourceAdapter`(fetch→normalize→upsert) + HTTP 하드닝(`_RateLimiter`·Retry·키 redaction)을 1-3/1-4/1-5/1-6이 재사용. 소스가 늘수록 스토리 비용이 감소.
2. **다층 리뷰가 실제 버그 검출** — 노이즈 아님: 1-7 YoY QoQ 오표기·음수분모 스크리너 오염, 1-5 raw_text wipe→전체교체, 1-6 분기 as_of 자연키 충돌. 여러 건은 리뷰가 아니었으면 놓쳤을 것.
3. **"null > 틀린 값" 원칙 일관** — 자유서식(1-5)·구조화(1-6) 모두 애매하면 null. 스크리너 데이터 신뢰도의 핵심.
4. **dev 전 스토리 점검이 지뢰 제거** — 1-5 C1(ZIP≠`_get`), 1-6 M2(보통주≠우선주)를 구현 전에 차단.
5. **테스트 안전망** — 1-6 코드리뷰 중 "특수관계인"의 "계" 부분문자열 오탐을 **자체 추가 테스트가 검출**.

## 어려웠던 것

1. **DART 공시 데이터가 지저분함 → 파싱 난이도** (리드 지적) — 1-2 total_debt 중복 라벨(하이닉스), 1-5 자유서식(구조화 API 없음), 감가상각비 부재→EBIT 근사. 실데이터가 가정을 반복적으로 깸.
2. **자유서식 파싱(1-5)이 최대 복잡도** — best-effort false-positive 지뢰밭. raw_text 보존이 안전망.
3. **git 저장소/경로 중간 변동** (리드 지적) — repo 위치·docs 통합·`{output_folder}` 리터럴 폴더가 중간에 정리됨. **착수 전 확정했어야 함.**
4. **리뷰 입력 커뮤니케이션 미스** (리드 지적) — 1-7 Blind Hunter에 코드를 축약 전달해 false positive 유발 → 이후 verbatim으로 교정.

## 핵심 교훈

- 컨텍스트 없는 리뷰어·외부 리뷰에 **코드 verbatim**(축약 금지) — 커뮤니케이션 미스·false positive 방지
- 스토리마다 **재사용 표** = 재발명 강력 차단
- **구조화 JSON(1-2/1-6) ≪ 자유서식(1-5)** 리스크 → 파싱 야심을 데이터 구조에 맞춤
- 한국어 **부분문자열 함정**(관계인/계) → 라벨은 정확일치
- **deferred-work 규율** — 수용된 한계를 기록, raw_text+전체교체로 재파싱 가능

## 리드(사용자) 회고

- DART 공시 데이터가 지저분해 파싱이 어려웠다.
- git 저장소 위치가 중간에 바뀜 — 미리 정리하고 시작했어야. **다음부터 경로 맞추고 시작.**
- 코드는 되도록 생략하지 말 것 — 커뮤니케이션 미스가 생긴다.

## 기술부채 / 이월 deferred (deferred-work.md 상세)

- 동시성 on_conflict upsert (전 어댑터 공통, 병렬화 시)
- PostgreSQL 통합 테스트로 AC5 이식성 실검증 (1-7)
- VIEW 성능 / materialized view 승격 (1-7, 운영 규모)
- 자유서식 깊은 문맥 파싱 (1-5, 실공시 샘플 후)
- `resp.json()` ValueError 처리를 dart.py/dart_valueup에 전파 (1-6서 수정, 나머지 미반영)
- EBITDA 감가상각비 출처 (1-2) → 2.3 M&A 정확도 직결
- 배당성향 vs 주주환원율 (1-5) → 2.1 갭 정확도 직결

## Epic 2 준비 — 밸류업 워싱 & M&A 스코어

**Epic 1 의존성 (준비됨)**: valuation_metrics 뷰(net_cash·ebitda_margin·ev_ebitda = M&A 입력), valueup_plan(갭 스코어), ownership(지배구조 스코어), financials 자사주 필드(워싱).

**새 패턴**: Epic 2는 **수집이 아니라 계산** — gap_engine·mna_engine이 writer(AD-4/AD-10). HTTP 어댑터 없음. 임계치·가중치는 config 주입(NFR3: 워싱 0.5/0.6, Value-up 0.5/0.3/0.2, M&A 0.35/0.25/0.25/0.15).

### 🚨 유의미한 발견 (Epic 2 착수 전 판단 필요)

1. **자사주 소각 데이터 공백** — `financials.buyback_retired_amount`는 별도공시라 best-effort null(1-2). 그런데 **2.2 워싱 플래그가 "NOT buyback_retired"에 의존**. 정밀 출처(`tesstkAcqsDspsSttus` 자기주식 취득/처분)는 1-6이 defer. → **2.1/2.2 착수 전 자사주 취득/소각 데이터 소스 확보 필요성 판단**.
2. **배당성향 ≠ 주주환원율** — 2.1 갭 스코어가 target_payout_ratio(배당성향) vs 실적 비교인데 공시는 주주환원율 발표(1-5 defer). 갭 왜곡 가능 → 2.1 설계 시 의미 확정.

## 액션 아이템

1. **착수 전 경로/repo 확정** (리드+AI) — 새 Epic/작업 착수 전 git repo 위치·아티팩트 경로(docs/)·BMAD config를 먼저 확정하고 시작. 중간 변동 금지.
2. **코드 verbatim 전달** (AI) — 컨텍스트 없는 리뷰어·GPT 번들·서브에이전트에 코드를 축약하지 않음.
3. **자사주 취득/소각 데이터 소스 판단** (리드) — 2.1/2.2 착수 전 `tesstkAcqsDspsSttus` 수집 스토리 필요 여부 결정.
4. **배당성향/주주환원율 의미 정합** (AI) — 2.1 갭 엔진 설계 시 target_payout_ratio 의미·매핑 확정.
5. **자유서식 파싱 튜닝은 실공시 샘플 후** (deferred) — raw_text 재파싱 활용.

## 준비도 평가

- 테스트/품질: 78 passed, 라이브 키 없이 CI 가능 ✅
- 배포: 미배포(로컬 개발). Epic 3에서 배포 예정.
- 코드 상태: 안정. 커밋 정리됨(docs 통합·1.7·1.5·1.6 push 완료).
- 블로커: 없음. 단, Epic 2는 위 '유의미한 발견' 2건을 착수 시 반영.
