# 1. 프로젝트 명

**밸류업 워싱 스크리너 (Value-up Washing Screener)**

> 개정 2026-08-04 (원판 2026-07-20). 원판은 **워싱 플래그(Washing Flag)를 핵심 산출물로
> 기재**했으나, 그 지표는 실측 검증 후 **폐기**되었다(사유는 §6 참조). 대체 축인
> **공시 불투명도(Opacity Rank)** 기준으로 전면 갱신.

------------------------------------------------------------------------

# 2. 프로젝트 개요

**AX 자동화 실습**

상장사의 **밸류업 계획 공시(DART)**와 **분기 재무제표, KRX 주가 데이터**를
통합하여 공시 내용과 실제 이행 여부를 비교·분석하는 금융 데이터 분석 시스템.

DART OpenAPI, pykrx, 금융공공데이터를 활용한 **데이터 파이프라인 구축**, SQL
기반 재무지표 계산과 **밸류업 갭 분석 엔진(Analysis Engine)**을 통해 계획 대비
실제 이행 수준을 **달성률(Achievement), 진척률(Progress), 밸류업 실행
점수(Execution Score), 공시 불투명도(Opacity Rank)**로 정량화.

**분석 대상은 밸류업 계획을 제출한 상장사 359개사 전수**이며, FastAPI 기반
**REST API**와 React 스크리너 화면, Tableau 대시보드로 제공한다.

**설계 원칙**: *판단할 수 없는 것을 판단한 척하지 않는다.* 수집–DB–API–화면
전 구간에서 `null`(모름)을 `0`이나 공란으로 치환하지 않으며, 이 원칙이
지표 설계 자체를 되돌린 사례가 §6에 있다.

------------------------------------------------------------------------

# 3. 사용 기술 및 도구

  ---------------------------------------------------------------------------------
  **카테고리**                        **보유 기술 / 활용 경험**
  ----------------------------------- ---------------------------------------------
  **프로그래밍 언어**                 Python, TypeScript

  **데이터 처리 및 분석**             Pandas, NumPy, SQL(DDL/DML), SQL VIEW, CTE,
                                      Window Function(LAG), 다중 JOIN, 재무지표
                                      계산(ROE·ROA·PBR·PER·EV/EBIT·부채비율·
                                      배당성향·총주주환원율·YoY), 백분위 스코어링

  **머신러닝 / 분석 엔진**            정량 스코어링(Scoring), Rule-based Analysis
                                      Engine, 동종업계 백분위 순위화

  **자연어 처리 / 생성형 AI**         DART 밸류업 공시 파싱, 정규식 기반 목표
                                      ROE·배당성향·총주주환원율·계획기간 추출,
                                      범위 표현 파싱, PDF 텍스트 추출 및 OCR
                                      (tesseract) 보조 수집 계층

  **웹서비스 구축**                   FastAPI, REST API, OpenAPI, SQLAlchemy 2.0,
                                      SQLite/PostgreSQL 호환, Uvicorn, Pydantic v2

  **프론트엔드**                      React, TypeScript, TanStack Query/Table,
                                      Zustand, Tailwind, Vite, Vitest

  **협업 / 개발환경**                 Git, GitHub, Alembic(28차 마이그레이션),
                                      pytest, httpx, 교차 코드리뷰 프로세스

  **AI 활용·자동화**                  APScheduler, DART OpenAPI, pykrx,
                                      금융공공데이터 API, 데이터 파이프라인 자동화,
                                      Tableau, Figma
  ---------------------------------------------------------------------------------

------------------------------------------------------------------------

# 4. 수행 역할 & 5. 수행 내용

> 기획–개발–검증–마감 **전 단계 단독 오너십**. 4주간(2026-07-08 ~ 08-04)
> 6개 에픽 33개 과제, 커밋 136건, 테스트 591건.

### 데이터 파이프라인 구축

-   DART OpenAPI, pykrx, 금융공공데이터 API 연동
-   APScheduler 기반 일배치 데이터 수집 자동화
-   기업 정보, 재무제표, 주가, 지분구조 통합 파이프라인 구축
-   corp_code와 stock_code 매핑 구조 설계
-   **유니버스 자동 발견** — DART 공시검색 API 스윕으로 밸류업 계획 제출사
    전수(359개사) 식별. 수작업 종목 선정을 제거
-   데이터 수집 예외 처리, 보조 원천 실패 격리(수집 실패가 재무 적재를 막지 않음)

### 데이터베이스 설계

-   SQLAlchemy 2.0 ORM 기반 설계, PostgreSQL 호환 유지
-   company, financials, prices, ownership, valueup_plan, valuation_metrics,
    valueup_score, mna_score, opacity_score 테이블 설계
-   Alembic 기반 마이그레이션 관리 **28차**
-   **파생지표는 SQL VIEW에서만 산출**하도록 아키텍처 원칙으로 고정 —
    앱 코드 재계산 금지로 지표 정합성을 구조적으로 담보

### SQL 기반 재무지표 분석

-   SQL VIEW 기반 `valuation_metrics` 설계
-   CTE, Window Function(LAG), 다중 JOIN 활용
-   ROE, ROA, PBR, PER, EV/EBIT, 부채비율, 배당성향, **총주주환원율**,
    YoY 성장률 계산
-   **look-ahead 차단 — 실제 공시일 기준**. 조회 시점(as_of)에 아직 공시되지 않은
    재무가 지표에 섞이지 않도록, DART 사업보고서 접수일(rcept_dt)을 전건 수집(706/706)해
    날짜로 판정. 차단 규칙은 6개 모듈에 복제돼 있던 것을 **단일 정의처로 이관**하고
    SQL판·Python판 전건 대조 테스트로 강제

### 밸류업 갭 분석 엔진 (Analysis Engine)

-   DART 밸류업 공시 파싱 — 목표 ROE, 배당성향, 총주주환원율, 계획기간 추출
-   **범위 표현 파싱**("ROE 11~13%") — 하한 채택 + 원문 범위 보존
-   계획 대비 실제 실적 비교, 달성률(Achievement)·진척률(Progress) 계산
-   **밸류업 실행 점수(Execution Score)** — 기업이 **공시한 축만으로** 채점하고,
    무엇을 근거로 매긴 점수인지(score_basis)를 점수와 함께 제공
-   **공시 불투명도(Opacity Rank)** — 목표 4축 중 미공시 축 수의 동종업계
    백분위. 고의를 판정하지 않고 **공시 격차**를 순위화
-   **목표 야심도** — 공시 목표를 자기 과거 실적·업종 중앙값과 비교한 격차
-   **자사주 3단계 판정** — 매입/소각/미실행 구분 + 소각 시점이 약속 전인지
    후인지 판정
-   **M&A 타겟 스코어** — 저평가·인수여력·지배구조·매크로 4요소 가중합,
    백분위 모집단(업종 내/시장 전체)을 점수와 함께 표기

### 결측 판정 체계 (핵심 설계)

-   재무 데이터의 "값이 없음"을 **6가지로 분해**하여 각각 다르게 처리
    -   업종 개념 부재(은행에 매출 개념 없음) / 계정명 변형(태그 폴백으로 회수) /
        다른 값에서 파생 가능 / 정당한 0 / 동음이의(‘현금및예치금’≠현금) /
        진짜 미상(null 유지)
-   **null 대 0 게이트** — "미공시"와 "당기 실적 0"을 구분. 반증 재료가
    존재하는 위치에 따라 판정 계층을 결정(같은 표 안 / 다른 표 대조 / 원문 재조회)
-   결과: 수집 커버리지 **순이익·자산·부채·현금 100%**, 배당 99%, 차입 94%

### REST API 구축

-   FastAPI 기반 REST API, 5개 라우터
-   다중조건 스크리닝 API(필터·정렬·페이지네이션·정렬 화이트리스트)
-   기업 상세 조회, 밸류업 갭 분석, M&A 타겟 랭킹, 시장·업종 통계 API
-   OpenAPI 문서 자동화 — **null 계약과 알려진 한계를 스키마 설명에 명시**

### 프론트엔드 (React 스크리너)

-   시장·업종·시총·지표 필터 + **사실 기반 필터 3종**
    (공시 불투명 상위 / 과거보다 낮은 목표 / 매입만 하고 소각 0)
-   밸류업 관점과 M&A 관점 탭 전환, 정렬 기준 자동 스왑
-   **불확실 상태를 점수와 동급으로 노출** — 판단 불가·미집계·산식 미적용을
    각각 다른 시각 언어로 구분
-   종목 상세 — 계획 대비 실적 갭, M&A 4요소 분해, 주주환원 구성 시계열,
    근거 공시 접수번호 표기
-   **배선 실증을 DoD로 명문화** — 목업 확인이 아니라 네트워크 로그·라이브 조작으로 검증

### 데이터 시각화

-   Tableau 대시보드 설계(원자적 스냅숏 + manifest 연결)
-   밸류업 실행 점수 랭킹, 업종별 저평가(PBR) 히트맵, ROE-PBR 산점도,
    배당 및 자사주 실이행 현황
-   Figma 기반 애널리스트 스크리너 UI 설계

------------------------------------------------------------------------

# 6. 성과 및 결과

### 정량 성과

  ------------------------------------------------------------------
  **항목**                       **결과**
  ------------------------------ -----------------------------------
  분석 대상                      **359개사** 전수 (재무 706행)

  밸류업 실행 점수 산출          **174종목**

  공시 불투명도 순위             **220종목**

  M&A 타겟 점수                  292종목 · **업종 내 비교 73%**

  수집 커버리지                  순이익·자산·부채·현금 **100%**,
                                 배당 99%, 자사주 96%, 차입 94%

  look-ahead 차단                실제 공시일 기준 · 사업보고서
                                 접수일 **706/706** 수집

  개발 규모                      6에픽 33과제 · 커밋 140+ · 4주

  테스트                         **601건** 전건 통과
                                 (백엔드 475 · 프론트 126)
  ------------------------------------------------------------------

### 구축 성과

-   DART, KRX, 금융공공데이터를 통합한 금융 데이터 파이프라인 구축
-   SQL VIEW, CTE, Window Function을 활용한 재무지표 계산 자동화
-   밸류업 갭 분석 엔진 설계 — 달성률·진척률·실행 점수·공시 불투명도·
    목표 야심도 기반 **다축 정량 평가 체계**
-   FastAPI REST API 및 React 스크리너·Tableau 연계 구조 구현
-   데이터 수집 → DB 구축 → 재무지표 분석 → 갭 분석 → REST API → 화면·시각화로
    이어지는 **End-to-End 금융 데이터 분석 시스템 구축**

### 핵심 성과 — 자기검증에 의한 지표 폐기

이 프로젝트의 **원래 간판 지표는 '워싱 플래그'**(공시 미이행 의심 판정)였다.

-   실데이터 투입 결과 **판정 True가 0건**이었다.
-   초기 진단은 "표본 33종목이라 근거가 부족하다"였고, 이를 확인하기 위해
    **유니버스를 359개사 전수로 확장**했다. **여전히 0건이었다.**
-   재진단 결과 **임계치 문제가 아니라 설계 결함**이었다 — 워싱 플래그는
    '고의'를 판정하는 지표인데, 재무 데이터로 고의는 판정할 수 없다.
    **켜질 수 없는 경고등**이었다.
-   지우기 전에 **대체 축을 먼저 구축**했다 — 공시 불투명도(고의를 판정하지 않고
    공시 격차를 재는 지표, 220종목에 순위 성립). 대체 작동을 확인한 뒤 은퇴시켰다.

**같은 성격의 자기 수정 3건**을 추가로 수행했다.

  --------------------------------------------------------------------------
  **발견**                         **내용**
  -------------------------------- -----------------------------------------
  결측 편향                        만점 비율 58% → **43%**. 58%는 이행하지
                                   않은 기업이 데이터 부족으로 채점에서
                                   빠진 편향이었다

  혼합 모집단                      감가상각 공시 기업만 다른 공식으로 재고
                                   있어, 순위가 **'공시했다는 사실'에 가점**을
                                   주고 있었다 → 정의 통일(EV/EBIT)

  단위 오류                        총주주환원율 계산에서 **금액(원)과
                                   수량(주)을 합산**하고 있었다 → 현금흐름표
                                   기준 금액으로 정정
  --------------------------------------------------------------------------

세 건 모두 실측으로 발견해 수정했으며, **그 결과 점수가 하락한 종목이 발생**했다.
지표를 방어하는 대신 지표의 정확성을 우선한 결과이며, **불확실을 확실로 포장하지
않는다는 설계 원칙이 산출물 수치보다 우선한다**는 것을 보여주는 사례다.

### 품질 확보

-   전 과제 **구현 모델 ↔ 리뷰 모델 교차검증**, 지적사항을 patch·defer·dismiss로
    triage하여 전량 문서화
-   리뷰어 처방보다 저비용 대안을 관철한 사례, 오탐을 사실관계로 기각한 사례 기록
-   **착수 전 재측정 규율** — 백로그에 기록된 원인 진단이 실측과 다른 경우가
    7회 발생. 시간이 지난 진단을 검증 없이 실행하지 않도록 프로세스화
-   마감 후 **CX 교차분석**(고객 여정 지도·Google HEART·IPA 3개 방법론 독립 수행)
    으로 한계 인식을 검증했고, 지목된 커버리지 항목을 이후 실제로 해소

### 남은 한계 (공개 기재)

-   **공시 첨부(PDF/HWP) 자동 수집 불가** — 대기업이 실계획을 첨부로 제출하는데
    DART OpenAPI로 취득할 수 없다. OCR 처리 계층은 구축했으나 취득이 수동
-   **실사용자 리서치 미수행** — CX 분석은 전문가 판정 추정치이며 이를 병기
